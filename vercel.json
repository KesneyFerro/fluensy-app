{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    },
    { "src": "backend/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/$1.js" }
  ]
}
